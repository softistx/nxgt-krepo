product: jvm/amper-plugin

dependencies:
  - //libs/stx-openapi-generator
  # swagger-parser logs through SLF4J; without a provider every build prints a warning
  - $libs.slf4j.nop

test-dependencies:
  - $libs.kotest.runner.junit5
  - $libs.kotest.framework.engine
  - $libs.kotest.assertions.core

pluginInfo:
  settingsClass: com.softistx.openapi.plugin.OpenApiSettings
