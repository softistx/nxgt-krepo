product: jvm/lib
description: "Ktor plugin for stx-graphix: one GraphQL engine per application, POST/GET /graphql"

apply:
  - //publishing.module-template.yaml

dependencies:
  - //libs/api/stx-graphix/stx-graphix: exported
  - $ktor.server.core: exported
  # graphql-ws on the GraphQL path. Not exported: the plugin installs the route.
  - $ktor.server.websockets
  # Ktor DI. Installing the plugin registers the engine with the container, so this is on the
  # runtime classpath of every application that installs it — and exported, because resolving what
  # was registered is the point.
  - $libs.ktor.server.di: exported

test-dependencies:
  - $ktor.server.testHost
  - $ktor.client.websockets
  - $libs.kotest.runner.junit5
  - $libs.kotest.framework.engine
  - $libs.kotest.assertions.core
  - $libs.slf4j.nop

settings:
  ktor:
    enabled: true
    version: 3.5.2
  kotlin:
    serialization: json
