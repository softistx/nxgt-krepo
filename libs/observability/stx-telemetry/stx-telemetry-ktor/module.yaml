product: jvm/lib
description: "Ktor plugin for stx-telemetry: one telemetry per application, and a server span per request that continues the incoming traceparent"

apply:
  - //libs/observability/stx-telemetry/stx-telemetry.module-template.yaml

dependencies:
  # Telemetry is what the plugin publishes and what call.telemetry returns
  - //libs/observability/stx-telemetry/stx-telemetry: exported
  - $ktor.server.core: exported
  # `resource`, `publish` and `required` — the lifecycle idiom every plugin in this repo shares
  - //libs/core/stx-ktor
  # Ktor DI. Installing the plugin registers the telemetry with the container, so this is on the
  # runtime classpath of every application that installs it — and exported, because resolving what
  # was registered is the point.
  - $libs.ktor.server.di: exported

test-dependencies:
  # StatusPages, to prove a failure it turns into a response is still an error on the span
  - $ktor.server.statusPages
  - $ktor.server.testHost
  - $libs.kotest.runner.junit5
  - $libs.kotest.framework.engine
  - $libs.kotest.assertions.core
  - $libs.slf4j.nop

settings:
  ktor:
    enabled: true
    version: 3.5.2
  kotlin:
    serialization: json
