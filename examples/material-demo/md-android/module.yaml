# An activity and a manifest. Same rule as the desktop launcher: nothing else belongs here.
product: android/app

dependencies:
  - //examples/material-demo/md-catalog
  # ComponentActivity and setContent — the only Android-specific thing the demo needs.
  - $libs.androidx.activity.compose

settings:
  compose:
    enabled: true
    version: 1.12.0
  android:
    namespace: com.softistx.material.demo.android
    applicationId: com.softistx.material.demo
