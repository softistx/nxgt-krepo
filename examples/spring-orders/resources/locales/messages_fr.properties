# French. Only the keys that differ need to be here — lookup walks per key, so anything missing
# falls back to the base catalog rather than switching the whole response to English.

orders.not-found           = Aucune commande avec l'identifiant {id}.
orders.reference-taken     = Une commande portant la référence {reference} existe déjà.
orders.unknown-status      = Ce statut est inconnu de ce service.

filters.invalid            = Ce filtre est illisible : {filter}

pagination.invalid         = Cette page ne peut pas être servie : {reason}

errors.unexpected          = Une erreur est survenue.
