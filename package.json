{
  "name": "nxgt-krepo",
  "version": "0.2.1",
  "private": true,
  "type": "module",
  "description": "Release tooling for the io.github.softistx:stx-* libraries. This repository is Kotlin; bun is here only to run Changesets.",
  "license": "Apache-2.0",
  "scripts": {
    "changeset": "changeset",
    "version": "changeset version && bun scripts/sync-version.ts",
    "typecheck": "tsc --noEmit",
    "test": "bun test scripts/"
  },
  "devDependencies": {
    "@changesets/changelog-github": "^1.0.1",
    "@changesets/cli": "^3.0.2",
    "@types/bun": "^1.4.0",
    "@types/node": "^26.5.0",
    "typescript": "6.0.3"
  }
}
