product: jvm/app

dependencies:
  - $libs.ktorfit.lib

settings:
  kotlin:
    ksp:
      processors:
        - $libs.ktorfit.ksp

plugins:
  openapi-client:
    enabled: true
    specFile: ../demo-api/openapi.yaml
    packageName: dev.nxgt.demo.client.api
