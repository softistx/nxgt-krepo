product: jvm/app

dependencies:
  - $libs.ktorfit.lib
  - $libs.kotlinx.datetime

settings:
  kotlin:
    serialization: json
    ksp:
      processors:
        - $libs.ktorfit.ksp

plugins:
  openapi-client:
    enabled: true
    specFile: ../demo-api/openapi.yaml
    packageName: dev.nxgt.demo.client.api
