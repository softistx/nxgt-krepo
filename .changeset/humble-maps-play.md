---
---

Comments and documentation only, nothing published changes: `stx-material`'s manifest and README
said `./kotlin build` and `./kotlin publish` behave the same way about the Apple targets. They do
not — `publish` cross-compiles both on Linux and `build` skips them — and the files now say what
was measured.
