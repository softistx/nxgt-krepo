---
"nxgt-krepo": patch
---

Specs only, nothing published changes: two harness timeouts now allow a slow start when CI runs every module's tests at once — `stx-graphix-spring`'s WebTestClient and the Ktor plugin specs' module-loading limit.
