---
---

Nothing published changes: the 45 artifacts still publish at 0.2.1, with the same POMs. This moves
where a version is written — from one line shared by every library to one per family — so that the
next release can move only the families that changed.
